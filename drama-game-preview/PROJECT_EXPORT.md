# 🎮 剧情织造机项目完整导出

## 📁 项目结构

```
drama-game-preview/
├── src/
│   ├── app/
│   │   ├── globals.css          # 全局样式文件
│   │   ├── layout.tsx           # 根布局组件
│   │   └── page.tsx             # 主页面组件
│   └── components/
│       └── DramaGameComponent.jsx  # 核心游戏组件（主要代码）
├── public/                       # 静态资源目录
├── .next/                       # Next.js 构建输出（可忽略）
├── node_modules/                # 依赖包（可忽略）
├── .cozeproj/
│   └── scripts/
│       ├── deploy_build.sh       # 构建脚本
│       └── deploy_run.sh         # 运行脚本
├── GUIDE.md                     # 完整游戏说明
├── QUICK_START.md               # 快速上手指南
├── EXAMPLES.md                  # 示例作品集
├── PROJECT_EXPORT.md            # 项目导出文档
├── README.md                    # 项目简介
├── package.json                 # 项目依赖配置
├── tsconfig.json               # TypeScript 配置
└── tailwind.config.js          # Tailwind CSS 配置
```

## 🚀 技术栈

### 前端框架
- **Next.js 16.0.10** - React 全栈框架
- **React 19.2.1** - 用户界面库
- **TypeScript 5.9.3** - 类型安全的 JavaScript

### 样式和UI
- **Tailwind CSS 4** - 实用优先的 CSS 框架
- **Lucide React 0.562.0** - 现代图标库
- **Geist Font** - Vercel 设计的现代字体

### 开发工具
- **ESLint 9** - 代码质量检查
- **PostCSS** - CSS 后处理器

---

## 💾 核心代码详解

### 1. 主页面组件 (src/app/page.tsx)

```typescript
'use client';

import React from 'react';
import DramaGameComponent from '@/components/DramaGameComponent';

export default function Home() {
  return (
    <div className="min-h-screen bg-gray-50">
      <header className="bg-white shadow-sm border-b">
        {/* 页面头部 */}
      </header>
      
      <main className="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
        {/* 主内容区域 */}
        <DramaGameComponent />
      </main>
      
      <footer className="bg-white border-t mt-8">
        {/* 页面底部 */}
      </footer>
    </div>
  );
}
```

**功能说明**：
- 使用 `'use client'` 指令启用客户端渲染
- 集成核心游戏组件 `DramaGameComponent`
- 响应式布局设计

### 2. 根布局组件 (src/app/layout.tsx)

```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

// 字体配置
const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
```

**功能说明**：
- 配置全局字体（Geist Sans 和 Geist Mono）
- 设置页面元数据
- 提供根布局结构

### 3. 全局样式 (src/app/globals.css)

```css
@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #ededed;
  }
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}
```

**功能说明**：
- 导入 Tailwind CSS
- 定义 CSS 自定义属性
- 支持暗色主题切换

### 4. 项目配置 (package.json)

```json
{
  "name": "drama-game-preview",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build", 
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "lucide-react": "^0.562.0",
    "next": "16.0.10",
    "react": "19.2.1",
    "react-dom": "19.2.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.0.10",
    "tailwindcss": "^4",
    "typescript": "5.9.3"
  }
}
```

**功能说明**：
- 定义项目依赖关系
- 配置开发和构建脚本
- 指定 TypeScript 和 ESLint

---

## 🎯 核心游戏组件 (DramaGameComponent.jsx) - V1.3.20

### 🔧 关键特性

#### 1. 数据持久化系统
```javascript
// 安全的LocalStorage操作函数
const safeLocalStorage = {
  get: (key, defaultValue = null) => {
    try {
      const item = localStorage.getItem(key);
      return item ? JSON.parse(item) : defaultValue;
    } catch (error) {
      console.error(`LocalStorage读取失败 [${key}]:`, error);
      return defaultValue;
    }
  },
  
  set: (key, value) => {
    try {
      localStorage.setItem(key, JSON.stringify(value));
      return true;
    } catch (error) {
      console.error(`LocalStorage写入失败 [${key}]:`, error);
      return false;
    }
  }
};
```

#### 2. 防抖保存机制
```javascript
const debouncedSave = (() => {
  let timeoutId = null;
  return (data, key) => {
    if (timeoutId) clearTimeout(timeoutId);
    timeoutId = setTimeout(() => {
      safeLocalStorage.set(STORAGE_KEYS[key], data);
      setLastSaveTime(Date.now());
    }, 500); // 500ms防抖延迟
  };
})();
```

#### 3. 智能匹配算法
```javascript
const scoredPlays = layerPlays.map(p => {
  const matchCount = p.tags.filter(t => scene.tags.includes(t)).length;
  return { ...p, score: matchCount };
});

const maxScore = Math.max(...scoredPlays.map(p => p.score));
const matchedPlays = maxScore > 0
  ? scoredPlays.filter(p => p.score === maxScore)
  : layerPlays;
```

#### 4. 双模式生成系统
```javascript
// 快速模式
const handleGenerateStory = () => {
  // 自动生成完整故事流程
};

// 沉浸模式
const startInteractiveMode = () => {
  // 逐层选择，完全控制故事走向
};
```

#### 5. [V1.3.20 新增] 世界观系统
```javascript
// 世界观筛选功能
const [selectedWorldview, setSelectedWorldview] = useState("");
const [enableWorldviewFilter, setEnableWorldviewFilter] = useState(false);

// 根据世界观过滤数据 [V1.3.20 修复]
const filterByWorldview = (data, type) => {
  if (!enableWorldviewFilter || !selectedWorldview) return data;
  
  return data.filter(item => {
    // 修复：当启用世界观筛选时，只显示匹配指定世界观的数据
    if (type === 'scene') return item.worldview === selectedWorldview;
    if (type === 'layer') return item.worldview === selectedWorldview;
    if (type === 'play') return item.worldview === selectedWorldview;
    if (type === 'command') return item.worldview === selectedWorldview;
    return false;
  });
};
```

#### 6. [V1.3.20 新增] 按世界观分组的ID生成
```javascript
const generateSequentialId = (prefix, existingIds, groupPrefix = '', type = 'default', worldviewName = '') => {
  const validIds = (existingIds || []).filter(id => id && typeof id === 'string');
  
  // [关键修复] 生成世界观分组前缀
  const worldviewPrefix = worldviewName ? chineseToFirstLetters(worldviewName).substring(0, 3) : '';
  const combinedPrefix = groupPrefix || worldviewPrefix || 'AAA';
  
  if (type === 'layer') {
    // 层级使用世界观前缀+英文+2位数字格式
    const fullPrefix = worldviewPrefix + combinedPrefix.substring(0, 2);
    // ... 生成唯一ID逻辑
  } else {
    // 其他类型（场景、玩法、指令）使用世界观前缀+首字母+3位数字格式
    const fullPrefix = worldviewPrefix + combinedPrefix.substring(0, 2);
    // ... 生成唯一ID逻辑
  }
};
```

### 📊 状态管理结构

```javascript
// 基础数据状态
const [scenes, setScenes] = useState(INITIAL_SCENES);
const [layers, setLayers] = useState(INITIAL_LAYERS);
const [plays, setPlays] = useState(INITIAL_PLAYS);
const [commands, setCommands] = useState(INITIAL_COMMANDS);
const [generatedHistory, setGeneratedHistory] = useState([]);

// UI交互状态
const [activeTab, setActiveTab] = useState("generator");
const [modalOpen, setModalOpen] = useState(false);
const [selectedSceneId, setSelectedSceneId] = useState("");
const [generatedStory, setGeneratedStory] = useState([]);

// [V1.3.20 新增] 世界观相关状态
const [selectedWorldview, setSelectedWorldview] = useState(""); // 选中的世界观
const [enableWorldviewFilter, setEnableWorldviewFilter] = useState(false); // 是否启用世界观过滤

// 沉浸模式状态
const [interactiveMode, setInteractiveMode] = useState(false);
const [interactiveStory, setInteractiveStory] = useState([]);
const [currentLayerIndex, setCurrentLayerIndex] = useState(0);
const [selectedChoices, setSelectedChoices] = useState([]);
const [selectedCommands, setSelectedCommands] = useState([]);
const [currentScene, setCurrentScene] = useState(null);

// 数据保存状态
const [isSaving, setIsSaving] = useState(false);
const [lastSaveTime, setLastSaveTime] = useState(null);
```

### 🎨 UI组件体系

#### 1. 基础组件
```javascript
const Card = ({ children, className = "", onClick }) => (
  <div onClick={onClick} className={`bg-white rounded-xl shadow-sm border border-slate-100 p-4 mb-3 ${className}`}>
    {children}
  </div>
);

const Badge = ({ children, color = "blue", className = "" }) => {
  const colors = {
    blue: "bg-blue-50 text-blue-700 border-blue-100",
    green: "bg-emerald-50 text-emerald-700 border-emerald-100",
    purple: "bg-purple-50 text-purple-700 border-purple-100",
    orange: "bg-orange-50 text-orange-700 border-orange-100",
    slate: "bg-slate-100 text-slate-600 border-slate-200",
  };
  return (
    <span className={`px-2 py-0.5 rounded-md text-xs font-medium border ${colors[color] || colors.slate} mr-1 ${className}`}>
      {children}
    </span>
  );
};
```

#### 2. 交互组件
```javascript
const Button = ({ children, onClick, variant = "primary", className = "", size = "md", disabled = false }) => {
  // 按钮样式和交互逻辑
};

// V1.3.19 修复：Modal 组件增加 zIndex 属性，用于控制层级，解决遮挡问题。
const Modal = ({ isOpen, onClose, title, children, zIndex = 50 }) => {
  // 模态框渲染逻辑，支持层级控制
};

const CheckboxList = ({ name, options, selectedValues, onChange, required = false }) => {
  // 多选框列表组件
};
```

### 🔍 数据模型 [V1.3.20 更新]

#### 场景模型
```javascript
{
  id: "SWYH001", // [修复] 世界观分组ID
  name: "废弃实验室",
  description: "充满灰尘和化学试剂味道的房间。",
  tags: ["恐怖", "解谜", "室内"],
  worldview: "世界观名称" // [新增] 世界观字段
}
```

#### 层级模型
```javascript
{
  layer_id: "LWYH01", // [修复] 世界观分组ID
  layer_name: "开场准备",
  sequence: 1,
  worldview: "世界观名称" // [新增] 世界观字段
}
```

#### 玩法模型
```javascript
{
  id: "PWYH001", // [修复] 世界观分组ID
  name: "搜寻物资",
  description: "在角落里翻找，发现了一个急救包。",
  trigger_condition: "无",
  result: "获得急救包",
  fk_layer_id: "L1_SETUP",
  tags: ["生存"],
  worldview: "世界观名称" // [新增] 世界观字段
}
```

#### 指令模型
```javascript
{
  id: "CWYH001", // [修复] 世界观分组ID
  name: "突发地震",
  description: "地面剧烈摇晃，物品掉落。",
  probability: 30,
  scope_type: "LAYER",
  fk_target_id: "L2_CORE",
  worldview: "世界观名称" // [新增] 世界观字段
}
```

---

## 🔧 部署脚本

### 构建脚本 (deploy_build.sh)
```bash
#!/bin/bash
set -Eeuo pipefail

WORK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "${WORK_DIR}/drama-game-preview"

# 检查环境
# 清理产物
# 安装依赖
# 构建项目
# 验证产物
```

### 运行脚本 (deploy_run.sh)
```bash
#!/bin/bash
set -Eeuo pipefail

WORK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

# 清理端口
# 启动服务
# 监听 DEPLOY_RUN_PORT
```

---

## 📋 使用说明

### 1. 开发环境启动
```bash
cd drama-game-preview
npm install
npm run dev
```

### 2. 生产环境部署
```bash
# 构建项目
bash /workspace/projects/.cozeproj/scripts/deploy_build.sh

# 启动服务
nohup bash /workspace/projects/.cozeproj/scripts/deploy_run.sh > output.log 2>&1 &
```

### 3. 数据备份
- 自动保存到浏览器 LocalStorage
- 支持导出 JSON 文件
- 支持分享链接备份

---

## 🎯 核心功能总结

### ✅ 已实现功能
1. **双模式生成**：快速模式 + 沉浸模式
2. **数据持久化**：自动保存 + 防抖机制
3. **智能匹配**：基于标签的玩法推荐
4. **批量操作**：支持批量导入场景、玩法、指令
5. **历史管理**：故事历史记录、导出、编辑
6. **响应式设计**：移动端适配
7. **数据导出**：JSON、TXT、分享链接
8. **用户体验**：进度条、状态指示、确认对话框
9. **[V1.3.20 新增] 世界观系统**：多世界观数据管理、筛选、导入导出
10. **[V1.3.20 新增] ID唯一性修复**：按世界观分组的唯一ID生成
11. **[V1.3.20 修复] 模态框层级**：解决模态框遮挡问题
12. **[V1.3.20 修复] 剧情模式交互**：修复突发指令多选和按钮显示问题

### 🔧 技术亮点
1. **防抖保存**：避免频繁 localStorage 操作
2. **错误处理**：完善的异常捕获和用户提示
3. **数据验证**：格式检查和默认值处理
4. **组件化**：高度模块化的 UI 组件
5. **类型安全**：TypeScript 配置和类型检查
6. **[V1.3.20 新增] 世界观分组ID**：避免不同世界观间ID冲突
7. **[V1.3.20 新增] 智能世界观推断**：自动识别和分类世界观数据
8. **[V1.3.20 新增] 模态框层级控制**：zIndex属性解决遮挡问题

### 📱 用户体验
1. **零学习成本**：直观的操作界面
2. **数据安全**：多重保存机制
3. **功能完整**：从创作到分享的全流程
4. **性能优化**：防抖、懒加载、缓存策略
5. **[V1.3.20 新增] 世界观管理**：支持多作品并行开发
6. **[V1.3.20 新增] 数据隔离**：不同世界观数据独立管理

### 🌟 V1.3.20 最新更新亮点

#### 1. 世界观系统全面上线
   - ✅ 支持多世界观数据管理
   - ✅ 智能世界观筛选和分类
   - ✅ 批量导入时自动世界观识别
   - ✅ 世界观标签功能，支持数据分类、筛选和导入导出
   - ✅ 在所有批量导入界面增加世界观输入框
   - ✅ **修复初始数据世界观字段**：确保演示数据包含正确的世界观字段，解决筛选功能失效问题

#### 2. ID重复问题彻底解决
   - ✅ 按世界观分组生成唯一ID
   - ✅ 中文转拼音前缀算法
   - ✅ 递增序号确保不冲突
   - ✅ 支持`assets/自己制作.json`等自定义数据导入的ID冲突修复

#### 3. 剧情模式交互优化（V1.3.20重点修复）
   - ✅ **修复突发指令多选问题**：解决React key冲突导致的多个指令同时被选中
   - ✅ **确保按钮显示逻辑**：剧情模式任何阶段都有确认和结束按钮
   - ✅ **优化key生成机制**：使用`interactive-cmd-${cmd.id}-layer-${currentLayerIndex}`唯一标识
   - ✅ **界面响应性改进**：无论是否有候选选项，都显示完整的操作按钮区域

#### 4. 用户体验优化
   - ✅ 模态框层级问题修复（zIndex控制）
   - ✅ 更直观的世界观选择器
   - ✅ 批量导入界面优化
   - ✅ 故事结束时的完整按钮控制

#### 5. 数据完整性增强
   - ✅ 世界观字段全覆盖
   - ✅ 数据一致性验证
   - ✅ 外键关联自动更新
   - ✅ 多数据集导入的ID冲突修复
   - ✅ **修复初始数据世界观缺失**：为INITIAL_SCENES、INITIAL_LAYERS、INITIAL_PLAYS、INITIAL_COMMANDS添加worldview字段，确保世界观筛选功能正常工作

---

## 🔧 关键修复详解（V1.3.20）

### 4. 世界观筛选场景显示问题修复 [V1.3.20 新增修复]

**问题现象**：
- 选择世界观后，场景没有按照世界观进行筛选显示
- 场景选择器仍然显示所有场景，而不是只显示匹配指定世界观的内容
- 世界观筛选功能完全失效

**根本原因**：
```javascript
// 问题1：初始演示数据缺少worldview字段
const INITIAL_SCENES = [
  { id: "S001", name: "废弃实验室", description: "充满灰尘和化学试剂味道的房间。", tags: ["恐怖", "解谜", "室内"] },
  { id: "S002", name: "中央公园", description: "阳光明媚，但似乎隐藏着危机。", tags: ["开放", "日间", "NPC"] }
];

// 问题2：filterByWorldview函数逻辑过于严格
const filterByWorldview = (data, type) => {
  if (!enableWorldviewFilter || !selectedWorldview) return data;
  
  return data.filter(item => {
    // 当item.worldview不存在时，返回false，导致没有世界观的数据被完全过滤
    if (type === 'scene') return item.worldview && item.worldview === selectedWorldview;
    // ...
  });
};
```

**修复方案**：
```javascript
// 修复1：为所有初始数据添加worldview字段
const INITIAL_SCENES = [
  { id: "S001", name: "废弃实验室", description: "充满灰尘和化学试剂味道的房间。", tags: ["恐怖", "解谜", "室内"], worldview: "默认世界观" },
  { id: "S002", name: "中央公园", description: "阳光明媚，但似乎隐藏着危机。", tags: ["开放", "日间", "NPC"], worldview: "默认世界观" }
];

const INITIAL_LAYERS = [
  { layer_id: "L1_SETUP", layer_name: "开场准备", sequence: 1, worldview: "默认世界观" },
  { layer_id: "L2_CORE", layer_name: "核心解谜", sequence: 2, worldview: "默认世界观" },
  { layer_id: "L3_ENDING", layer_name: "结局判定", sequence: 3, worldview: "默认世界观" }
];

const INITIAL_PLAYS = [
  { id: "P001", name: "搜寻物资", description: "在角落里翻找，发现了一个急救包。", trigger_condition: "无", result: "获得急救包", fk_layer_id: "L1_SETUP", tags: ["生存"], worldview: "默认世界观" },
  // ... 其他数据
];

const INITIAL_COMMANDS = [
  { id: "C001", name: "突发地震", description: "地面剧烈摇晃，物品掉落。", probability: 30, scope_type: "LAYER", fk_target_id: "L2_CORE", worldview: "默认世界观" },
  // ... 其他数据
];

// 修复2：优化filterByWorldview函数逻辑
const filterByWorldview = (data, type) => {
  if (!enableWorldviewFilter || !selectedWorldview) return data;
  
  return data.filter(item => {
    // 修复：当启用世界观筛选时，只显示匹配指定世界观的数据
    // 移除了过于严格的item.worldview &&检查，允许精确匹配
    if (type === 'scene') return item.worldview === selectedWorldview;
    if (type === 'layer') return item.worldview === selectedWorldview;
    if (type === 'play') return item.worldview === selectedWorldview;
    if (type === 'command') return item.worldview === selectedWorldview;
    return false;
  });
};
```

**修复效果**：
- ✅ 世界观筛选功能正常工作
- ✅ 选择世界观后，场景列表只显示匹配的场景
- ✅ 初始演示数据包含正确的世界观字段
- ✅ 支持多世界观数据的正确筛选和显示
- ✅ 在生成器和剧情模式中都能正确应用世界观筛选

**验证步骤**：
1. 启用世界观筛选
2. 选择"默认世界观"
3. 查看场景选择器，应只显示属于"默认世界观"的场景
4. 生成故事时，玩法和指令也按世界观筛选
- ✅ 筛选功能完全正常，数据按世界观正确分类显示

---

### 1. 剧情模式突发指令多选问题修复

**问题现象**：
- 在剧情走向模式中，点击一个突发指令会导致多个指令同时被选中
- 复选框状态混乱，用户无法精确选择单个指令

**根本原因**：
```javascript
// 问题代码：包含状态的key导致React虚拟DOM混乱
key={`cmd-${cmd.id}-${currentLayerIndex}-${selectedCommands.includes(cmd.id) ? 'checked' : 'unchecked'}`}
```

**修复方案**：
```javascript
// 修复后：使用稳定的唯一key
key={`interactive-cmd-${cmd.id}-layer-${currentLayerIndex}`}
```

**修复效果**：
- ✅ 消除React key冲突
- ✅ 复选框状态正常
- ✅ 用户可以精确选择单个或多个指令
- ✅ 界面交互流畅无异常

### 2. 确保剧情模式按钮完整显示

**问题现象**：
- 在某些剧情阶段，用户既没有确认按钮也没有结束按钮
- 生成故事3个场景后界面卡住，无法继续或结束

**修复方案**：
```javascript
// 修复前：只有有候选选项时才显示按钮
{(candidatePlays.length > 0 || candidateCommands.length > 0) && (
  <div className="bg-white rounded-xl shadow-lg border border-slate-200 p-6">
    {/* 选择选项 */}
    <div className="grid grid-cols-2 gap-3">
      <Button>确认选择</Button>
      <Button>结束故事</Button>
    </div>
  </div>
)}

// 修复后：始终显示操作按钮区域
<div className="bg-white rounded-xl shadow-lg border border-slate-200 p-6">
  {/* 只有有候选选项时才显示选择部分 */}
  {(candidatePlays.length > 0 || candidateCommands.length > 0) ? (
    <div>
      <h3>选择你的行动</h3>
      {/* 候选选项列表 */}
    </div>
  ) : (
    <div className="text-center py-6">
      <div>当前没有可选的行动</div>
      <div>你可以直接结束故事或返回上级</div>
    </div>
  )}
  
  {/* 操作按钮 - 始终显示 */}
  <div className="grid grid-cols-2 gap-3 mt-6">
    <Button>确认选择</Button>
    <Button>结束故事</Button>
  </div>
</div>
```

**修复效果**：
- ✅ 任何阶段都有确认和结束按钮
- ✅ 没有候选选项时显示友好提示
- ✅ 用户可以随时结束故事
- ✅ 避免界面卡死情况

### 3. assets/自己制作.json导入ID重复问题修复

**问题现象**：
- 导入`assets/自己制作.json`等自定义数据时出现ID重复
- 不同数据集间的ID冲突导致数据覆盖或显示异常

**修复方案**：
```javascript
// 关键修复：为每个数据集生成唯一且递增的ID
const importFromCustomJson = (jsonContent, currentData) => {
  const data = typeof jsonContent === 'string' ? JSON.parse(jsonContent) : jsonContent;
  
  data.forEach((dataset, datasetIndex) => {
    // [修复] 为每个数据集提取世界观名称（优先使用worldview字段）
    let worldviewName = "";
    if (dataset.worldview) {
      worldviewName = dataset.worldview;
    } else if (dataset.scenes && dataset.scenes.length > 0 && dataset.scenes[0].name) {
      // 从场景名称中提取世界观
      worldviewName = dataset.scenes[0].name.includes('王勇和体育生') ? '王勇和体育生故事' : 
                     dataset.scenes[0].name.includes('月王') ? '月王故事' : 
                     dataset.scenes[0].name;
    }
    
    // [修复] 获取当前数据集的首字母作为ID前缀
    let groupPrefix = '';
    if (dataset.scenes && dataset.scenes.length > 0 && dataset.scenes[0].name) {
      groupPrefix = chineseToFirstLetters(dataset.scenes[0].name);
    }
    
    // 为每个数据项生成唯一ID
    const scenesWithNewIds = dataset.scenes.map((scene, index) => ({
      ...scene,
      id: generateSequentialId('S', existingSceneIds, groupPrefix, 'scene', worldviewName),
      worldview: worldviewName
    }));
    
    // 类似处理layers、plays、commands...
  });
};
```

**修复效果**：
- ✅ 彻底解决ID重复问题
- ✅ 支持多数据集并行导入
- ✅ 智能世界观识别和分类
- ✅ 保持数据完整性和一致性

---

## 📞 技术支持

### 文档资源
- `GUIDE.md` - 完整游戏说明
- `QUICK_START.md` - 快速上手指南  
- `EXAMPLES.md` - 示例作品集

### 核心代码位置
- **主逻辑**：`src/components/DramaGameComponent.jsx` (1500+ 行，V1.3.20)
- **页面入口**：`src/app/page.tsx`
- **全局样式**：`src/app/globals.css`
- **项目配置**：`package.json`、`tsconfig.json`

### 关键文件说明
- **DramaGameComponent.jsx**：核心业务逻辑，包含所有功能实现
- **deploy_build.sh**：项目构建脚本
- **deploy_run.sh**：服务运行脚本
- **assets/**：示例数据文件，包含多世界观测试数据

---

*项目导出完成 | 剧情织造机 V1.3.20*  
*导出时间：2025年1月*  
*核心代码：DramaGameComponent.jsx (主要业务逻辑)*  
*最新特性：世界观系统 + ID唯一性修复 + 模态框层级优化 + 剧情模式交互修复*